<%= simple_form_for(review, url: account_place_places_reviews_path(place)) do |f| %>
  <%= f.error_notification %>
  <%= f.input :rating %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>
